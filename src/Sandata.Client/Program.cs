// Entry point placeholder. Task 14 of
// docs/plans/2026-08-07-sandata-scaffold.md replaces this with the real client
// bootstrap, and task 33 gives it a window to open. It exists now only because
// a WinExe project without an entry point fails the build with CS5001, and
// task 1 has to leave the solution green.
return 0;
